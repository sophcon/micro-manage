@{
    Layout = null;
}

<form>
    <div class="form-group">
        <label>Name</label>
        
        @Html.TextBox("Name", null, new { @class = "form-control", @placeholder = "name" })
    </div>
    <div class="form-group">
        <label>Category</label>
        @Html.TextBox("Category", null, new { @class= "form-control", @placeholder = "name"})
    </div>

</form>
