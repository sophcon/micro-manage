

<table class="table table-condensed table-responsive">
    <thead>
        <tr>
            <td>Name</td>
            <td>Available</td>
            <td>price</td>
            <td>Quantity</td>
        </tr>
    </thead>
    <tbody>
        <tr ng-repeat="product in products">
            <td>{{product.Name}}</td>
            <td>{{product.WebAvailble}}</td>
            <td>{{product.Price}}</td>
            <td>{{product.Count}}</td>
        </tr>
    </tbody>
</table>
<div ></div>