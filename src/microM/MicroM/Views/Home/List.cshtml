<div class="form-group">
    {{message}}
    <input class="form-control" ng-model="message"/>
    <button class="btn btn-primary" ng-click="send()">update</button>
</div>

<div ng-repeat="product in products">{{product}}</div>